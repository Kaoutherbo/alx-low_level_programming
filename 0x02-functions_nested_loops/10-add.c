#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @num1: parameter to be checked
 * @num2: parameter to be checked
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int add(int num1 , int num2)
{
	return (num1+num2);
}
