#include "main.h"


/**
 * mul - function that multiplies two integers.
 * @a: parameter to be checked.
 * @b: parameter to be chacked.
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int  mul(int a, int b)
{
	return (a * b);
}
